# LoggerViewer (LV) - Web-просмотрщик логов

## 📌 Название проекта
**LV** (LoggerViewer) - легковесный web-сервер для просмотра и анализа лог-файлов через браузер.

## 🏗️ Архитектура проекта

### Компоненты
- **Frontend**: HTML/JavaScript интерфейс (встроенный через `//go:embed`)
- **Backend**: Go HTTP сервер с REST API
- **Конфигурация**: Параметры командной строки с значениями по умолчанию

### API Endpoints

| Метод | Путь                     | Назначение                          |
|-------|--------------------------|-------------------------------------|
| `GET` | `/`, `/index.html`       | Веб-интерфейс                       |
| `GET` | `/api/health`            | Проверка состояния сервера          |
| `GET` | `/api/files`             | Список всех лог-файлов              |
| `GET` | `/api/file?path=...`     | Содержимое файла (с предпросмотром) |
| `GET` | `/api/download?path=...` | Скачивание полного файла            |
| `GET` | `/api/config`            | Текущая конфигурация                |

### Особенности
- Автоматическое создание каталога логов при отсутствии
- Поддержка кодировок: UTF-8, Windows-1251, KOI8-R, ISO-8859-1, CP866
- GZIP сжатие для всех ответов
- CORS поддержка для кросс-доменных запросов
- Ограничение предпросмотра больших файлов

## 🛠️ Сборка проекта

### Требования
- Go 1.16 или выше
- Доступ к интернету для загрузки зависимостей

### Команды сборки

#### Windows x64
```bash
# Windows x64
GOOS=windows GOARCH=amd64 go build -o bin/win/lv.exe main.go
# Linux x64
GOOS=linux GOARCH=amd64 go build -o bin/linux/lv main.go
# macOS (Apple Silicon)
GOOS=darwin GOARCH=arm64 go build -o bin/mac/a/lv main.go
# macOS (Intel)
GOOS=darwin GOARCH=amd64 go build -o bin/mac/i/lv main.go
```

## 📖 Использование

### Вызов помощи
```bash
./lv -h
```
или
```bash
./lv.exe -h
```

### Параметры командной строки

| Параметр         | По умолчанию     | Описание                                                     |
|------------------|------------------|--------------------------------------------------------------|
| `-dir`           | `./logs`         | Каталог с лог-файлами                                        |
| `-preview-limit` | `1048576` (1 МБ) | Макс. размер для предпросмотра (байты)                       |
| `-port`          | `7424`           | Порт HTTP сервера                                            |
| `-encoding`      | `utf-8`          | Кодировка файлов (utf-8, win1251, koi8-r, iso-8859-1, cp866) |


После запуска сервер выведет информацию о конфигурации:
```
🚀 Web-просмотрщик логов (версия: 1.0.0)
📁 Каталог логов: ./logs
📏 Макс. размер для предпросмотра: 1048576 байт (1.00 МБ)
🔤 Кодировка файлов: utf-8
🌐 Порт сервера: 7424
🌐 Сервер запущен: http://localhost:7424
📱 Веб-интерфейс доступен по корневому URL
🛑 Для остановки сервера нажмите Ctrl+C
```

### 🌐 Доступ к интерфейсу

Откройте браузер и перейдите по адресу:
```
http://localhost:7424
```

### Примеры запуска

#### 1. Запуск с параметрами по умолчанию
```bash
./lv
```
- Каталог логов: `./logs` (относительно исполняемого файла)
- Порт: `7424`
- Макс. предпросмотр: `1 МБ`
- Кодировка: `UTF-8`

#### 2. Запуск с пользовательскими параметрами
```bash
./lv -dir /var/log/myapp -preview-limit 5242880 -port 8080 -encoding win1251
```
- Каталог логов: `/var/log/myapp`
- Макс. предпросмотр: `5 МБ`
- Порт: `8080`
- Кодировка: `Windows-1251`

#### 3. Ещё один пользовательский запуск (запуск из README.md)
```bash
bin/mac/a/lv -dir=/Users/valentin/Downloads/log 
```

#### 4. Запуск на Windows с указанием пути
```bash
lv.exe -dir "C:\ProgramData\MyApp\Logs"
```

#### 5. Запуск для просмотра больших файлов
```bash
./lv -preview-limit 10485760  # 10 МБ лимит
```

### Автоматический запуск

#### (Опционально) Загрузка утилиты на удалённый сервер
```bash
#scp bin/linux/lv oem@vm-pshb-new.bss.lan:/opt/tomcat_app/apache-tomcat-9.0.105/

scp bin/linux/lv oem@vm-pshb-new.bss.lan:/tmp/ && \
ssh oem@vm-pshb-new.bss.lan "sudo systemctl stop lv && cp /tmp/lv /opt/tomcat_app/apache-tomcat-9.0.105 && sudo systemctl start lv && sudo systemctl status lv --no-pager --lines=10"

```

#### Пример настройки службы в Linux
```bash
sudo tee /etc/systemd/system/lv.service > /dev/null << 'EOF' && \
sudo systemctl daemon-reload && \
sudo systemctl start lv && \
sudo systemctl enable lv && \
echo "Service created and started successfully"
[Unit]
Description=LoggerViewer (LV) - Web-просмотрщик логов

[Service]
Type=simple
User=oem
ExecStart=/opt/tomcat_app/apache-tomcat-9.0.105/lv
Restart=always
WorkingDirectory=/opt/tomcat_app/apache-tomcat-9.0.105/

[Install]
WantedBy=multi-user.target
EOF
```
#### Просмотр журнала службы в Linux
```bash
# Журнал systemd
sudo journalctl -u lv.service          # Логи службы
sudo journalctl -u lv.service -f       # Следить в реальном времени
sudo journalctl -u lv.service --since "1 hour ago"
sudo journalctl -u lv.service -n 100   # Последние 100 строк
sudo journalctl -u lv.service -p err   # Только ошибки

# Детальная информация:
sudo systemctl show lv.service         # Все параметры службы
sudo systemctl cat lv.service          # Показать конфиг файл
```

## 🔒 Безопасность

- Все пути проверяются на нахождение внутри разрешенного каталога
- Нет доступа к файлам вне каталога логов
- Все ответы сжимаются с использованием GZIP
- Поддерживаются только GET запросы к API

## 📦 Зависимости

Проект использует стандартную библиотеку Go и:
- `golang.org/x/text` - для поддержки различных кодировок

Зависимости загружаются автоматически при первой сборке.

## 🐛 Отладка

При возникновении проблем:
1. Проверьте права доступа к каталогу логов
2. Убедитесь, что порт не занят другим приложением
3. Проверьте кодировку лог-файлов (должна соответствовать параметру `-encoding`)
4. Посмотрите логи сервера в консоли

---

**Примечание**: Проект предназначен для локального использования или внутри защищенных сетей. Не рекомендуется открывать доступ к серверу из интернета без дополнительных мер безопасности.

2026, [va.slyusarev@gmail.com](mailto:va.slyusarev@gmail.com)